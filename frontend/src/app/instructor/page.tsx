"use client";

// The quality workspace is deliberately available outside /admin: course
// membership, rather than the global administrator role, authorizes its APIs.
export { default } from "../admin/course-quality/page";
